module.exports = {
    getImageItem: (id) => {
        return `https://site.daro.fr/getImg/large/${id}.gif`
    },
    getImageMap: (name) => {
        return `https://site.daro.fr/getImg/maps_xl/${name}.gif`
    }
}